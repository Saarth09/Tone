# Tone monorepo on Railway — create two services from this repo.
#
# Backend service:
#   Root Directory: /backend
#   Config file: /backend/railway.toml
#   Volume mount: /app/data
#
# Frontend service:
#   Root Directory: /frontend
#   Config file: /frontend/railway.toml
#   Variable: VITE_API_URL=https://<backend-public-url>
#
# Do not put a [build] dockerfilePath here — it would apply to every service.
