server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing — API calls go to VITE_API_URL (absolute) from the browser.
    location / {
        try_files $uri /index.html;
    }
}
